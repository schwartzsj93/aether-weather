{
  "name": "aether-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Streaming Anthropic proxy — Lambda Function URL handler",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  }
}
